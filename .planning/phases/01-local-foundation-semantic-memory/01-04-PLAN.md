---
phase: 01-local-foundation-semantic-memory
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - apps/server/package.json
  - apps/server/src/db/zvec.ts
  - apps/server/src/routes/memories.ts
  - apps/server/src/index.ts
  - packages/env/src/server.ts
autonomous: true
requirements:
  - MEM-04
  - UI-01
gap_closure: true
must_haves:
  truths:
    - "Starting the Hono backend server auto-creates the directory and Zvec database file at ~/.job-auto-apply/memory.db"
    - "The popup header displays a real-time connection status reflecting the backend's availability"
  artifacts:
    - path: "apps/server/src/db/zvec.ts"
      provides: "Zvec database initialization and setup"
    - path: "apps/server/src/index.ts"
      provides: "CORS configuration allowing extension origin"
  key_links:
    - from: "apps/server/src/index.ts"
      to: "apps/server/src/db/zvec.ts"
      via: "Database initialization call"
      pattern: "initDb()"
---

<objective>
Close UAT gaps regarding vector database implementation and extension CORS configuration.

Purpose: To ensure the intended `zvec` vector database is used and that the extension can actually connect to the Hono backend without CORS errors.
Output: Working Zvec database integration and properly configured CORS.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
@./.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/server/src/db/zvec.ts
@apps/server/src/index.ts
@packages/env/src/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate backend from LanceDB to Zvec</name>
  <files>
    apps/server/package.json
    apps/server/src/db/zvec.ts
    apps/server/src/routes/memories.ts
  </files>
  <action>
    - Remove `@lancedb/lancedb` dependency from `apps/server/package.json` and install `zvec`.
    - Update `apps/server/src/db/zvec.ts` (if `db.ts` exists, consolidate logic here and delete `db.ts`) to use `zvec` instead of `@lancedb/lancedb`.
    - Change the storage path variable (`DB_PATH`) from `~/.job-auto-apply/lancedb` to `~/.job-auto-apply/memory.db`.
    - Update the `initDb()` function to initialize the Zvec database and auto-create the directory if missing.
    - Update database interactions in `apps/server/src/routes/memories.ts` (e.g., adding, searching, querying, and deleting vectors) to match the `zvec` API signature.

    Gap reason: The developer incorrectly implemented LanceDB instead of the planned Zvec DB and used the wrong storage path.
  </action>
  <verify>
    <automated>cd apps/server && bun run check-types || echo "Setup types"</automated>
  </verify>
  <done>Zvec is successfully integrated, storing data at `~/.job-auto-apply/memory.db`.</done>
</task>

<task type="auto">
  <name>Task 2: Update CORS configuration for Chrome Extension</name>
  <files>
    apps/server/src/index.ts
    packages/env/src/server.ts
  </files>
  <action>
    - Update the CORS middleware configuration in `apps/server/src/index.ts`.
    - Instead of rigidly using a string `env.CORS_ORIGIN`, change it to allow requests from `env.CORS_ORIGIN` AND any origin that starts with `chrome-extension://`.
    - E.g., `origin: (origin) => (origin && (origin.startsWith('chrome-extension://') || origin === env.CORS_ORIGIN)) ? origin : env.CORS_ORIGIN`.
    - Update `packages/env/src/server.ts` if needed, although adapting the check at runtime in Hono is generally sufficient.

    Gap reason: The extension's `useBackendStatus` hook fails to communicate with the local Hono backend because the default `http://localhost:5173` strict origin blocks `chrome-extension://` requests.
  </action>
  <verify>
    <automated>cd apps/server && bun run check-types || echo "Setup types"</automated>
  </verify>
  <done>CORS middleware in the backend accepts requests from `chrome-extension://` origins.</done>
</task>

</tasks>

<verification>
Ensure the server boots up normally with Zvec integration and that a test HTTP request mimicking a chrome extension origin returns successful CORS headers.
</verification>

<success_criteria>
- Zvec replaces LanceDB and `memory.db` file is created locally on startup.
- Chrome extension frontend can successfully access `http://localhost:3000/health` without CORS blockades.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-foundation-semantic-memory/01-04-SUMMARY.md`
</output>

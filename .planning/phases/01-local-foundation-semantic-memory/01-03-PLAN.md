---
phase: 01-local-foundation-semantic-memory
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/extension/wxt.config.ts
  - apps/extension/package.json
  - apps/extension/entrypoints/popup/App.tsx
  - apps/extension/hooks/useBackendStatus.ts
  - apps/extension/entrypoints/popup/Memories.tsx
autonomous: true
requirements:
  - UI-01
  - MEM-02
must_haves:
  truths:
    - "Extension popup shows backend online/offline status"
    - "Extension popup has tabbed navigation"
    - "User can view, edit, and delete memories in the UI"
  artifacts:
    - path: "apps/extension/entrypoints/popup/App.tsx"
      provides: "Main popup UI"
    - path: "apps/extension/hooks/useBackendStatus.ts"
      provides: "Backend health polling"
    - path: "apps/extension/entrypoints/popup/Memories.tsx"
      provides: "Memories management UI"
  key_links:
    - from: "apps/extension/hooks/useBackendStatus.ts"
      to: "http://localhost:3000/health"
      via: "fetch"
    - from: "apps/extension/entrypoints/popup/Memories.tsx"
      to: "http://localhost:3000/api/memories"
      via: "fetch"
---

<objective>
Scaffold the WXT React Chrome extension and implement the basic popup UI with Hono backend status awareness, tabbed navigation, and a Memories management UI.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Scaffold WXT React Extension</name>
  <files>apps/extension/package.json, apps/extension/wxt.config.ts</files>
  <action>Setup apps/extension with WXT and React. Add Tailwind CSS for styling. Configure wxt.config.ts for Manifest V3.</action>
  <verify><automated>cd apps/extension && bun run build</automated></verify>
  <done>Extension builds successfully without dev server hanging.</done>
</task>
<task type="auto">
  <name>Task 2: Implement Tabbed Popup UI & Backend Status</name>
  <files>apps/extension/entrypoints/popup/App.tsx, apps/extension/hooks/useBackendStatus.ts</files>
  <action>Build App.tsx with a bottom tab bar (Main, Memories, Resumes, Settings). Implement Main view with large "Start Auto-Fill" button and provider dropdown. Create useBackendStatus.ts hook that polls http://localhost:3000/health and display Online/Offline status in the header.</action>
  <verify><automated>cd apps/extension && bun run build</automated></verify>
  <done>App component builds with navigation structure and backend status hook compiles correctly.</done>
</task>
<task type="auto">
  <name>Task 3: Implement Memories UI</name>
  <files>apps/extension/entrypoints/popup/Memories.tsx, apps/extension/entrypoints/popup/App.tsx</files>
  <action>Build Memories component allowing users to view a list of saved memories, edit existing memories (question and answer), and delete memories. Wire this component into the Memories tab in App.tsx. Use the GET, PATCH, and DELETE endpoints from the backend API.</action>
  <verify><automated>cd apps/extension && bun run build</automated></verify>
  <done>Memories UI is implemented and integrated without build errors.</done>
</task>
</tasks>

<success_criteria>
Extension popup is functional, communicates with local backend, and allows memory management.
</success_criteria>

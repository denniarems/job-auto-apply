---
phase: 01-local-foundation-semantic-memory
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/server/src/routes/memories.ts
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03
must_haves:
  truths:
    - "Can store memory with question/answer"
    - "Can search memories by semantic similarity"
  artifacts:
    - path: "apps/server/src/routes/memories.ts"
      provides: "Memory CRUD and search API"
  key_links:
    - from: "apps/server/src/routes/memories.ts"
      to: "apps/server/src/db/lancedb.ts"
      via: "database queries"
---

<objective>
Create the Hono API endpoints for storing and searching semantic memories using LanceDB vector similarity.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/server/src/db/lancedb.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement Memory Store API</name>
  <files>apps/server/src/routes/memories.ts</files>
  <action>Create POST /api/memories. Use Vercel AI SDK (embed method) to generate 768-dim embeddings from question. Store question, answer, embedding, source, usage_count, and last_used timestamps.</action>
  <verify><automated>cd apps/server && bunx hono request src/index.ts -X POST /api/memories -H "Content-Type: application/json" -d '{"question":"test","answer":"test"}' || echo "Need test script"</automated></verify>
  <done>Memory stored in LanceDB.</done>
</task>
<task type="auto">
  <name>Task 2: Implement Semantic Search API</name>
  <files>apps/server/src/routes/memories.ts</files>
  <action>Create GET /api/memories/search with query param. Perform LanceDB vector search with 85% fixed threshold. Increment usage_count and update last_used on successful search match.</action>
  <verify><automated>cd apps/server && bunx hono request src/index.ts /api/memories/search?query=test || echo "Need test script"</automated></verify>
  <done>Search returns relevant results.</done>
</task>
<task type="auto">
  <name>Task 3: Implement Memory Management API</name>
  <files>apps/server/src/routes/memories.ts</files>
  <action>Create GET /api/memories/all, DELETE /api/memories/:id, and PATCH /api/memories/:id. Support basic pagination or sorting by last_used. The PATCH endpoint should allow updating question and/or answer, and should regenerate the embedding if the question is updated.</action>
  <verify><automated>cd apps/server && bunx hono request src/index.ts -X DELETE /api/memories/1 || echo "Need test script"</automated></verify>
  <done>Delete successfully removes memory, and PATCH successfully updates memory.</done>
</task>
</tasks>

<success_criteria>
API supports full lifecycle of semantic memories including editing.
</success_criteria>

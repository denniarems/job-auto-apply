# Plan 01-01: Backend Foundation & Zvec Integration

**Objective**: Establish the local-first Hono backend and initialize the Zvec vector database with persistence to the user's home directory.

## Phase Reference
Phase 01: Local Foundation & Semantic Memory
Context: `.planning/phases/01-local-foundation-semantic-memory/01-CONTEXT.md`

## Requirements
- **MEM-04**: Securely store all memories locally on the user's machine.
- **AI-03**: Securely manage API keys in a local backend `.env` file.

## Tasks

### 1. Initialize Hono Server & Environment
- **Action**: Setup `apps/server` with Hono and Bun.
- **Action**: Configure `.env` validation using Zod in `packages/env` to include AI provider keys.
- **Verification**: `curl http://localhost:3000/health` returns 200 OK.

### 2. Configure Zvec Database
- **Action**: Initialize Zvec in `apps/server/src/db/zvec.ts`.
- **Action**: Set storage path to `~/.job-auto-apply/memory.db` (auto-create dir).
- **Action**: Create `memories` and `form_templates` collections.
- **Verification**: Check if `~/.job-auto-apply/memory.db` file is created on startup.

### 3. Implement Health & Status Endpoints
- **Action**: Add `/health` endpoint that checks Zvec connection and `.env` key presence.
- **Verification**: Health endpoint returns JSON with `database: "connected"` and `keys: { anthropic: true, ... }`.

---
*Created: 2026-02-25*

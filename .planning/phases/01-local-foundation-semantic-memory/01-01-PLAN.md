---
phase: 01-local-foundation-semantic-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/package.json
  - apps/server/src/index.ts
  - apps/server/src/db/zvec.ts
  - packages/env/package.json
  - packages/env/src/server.ts
autonomous: true
requirements:
  - MEM-04
  - AI-03
must_haves:
  truths:
    - "Hono server starts successfully"
    - "Zvec database initializes and creates memory.db in ~/.job-auto-apply"
    - "Health endpoint returns database and keys status"
  artifacts:
    - path: "apps/server/src/index.ts"
      provides: "Hono server entrypoint"
    - path: "apps/server/src/db/zvec.ts"
      provides: "Zvec database initialization"
  key_links:
    - from: "apps/server/src/index.ts"
      to: "apps/server/src/db/zvec.ts"
      via: "Initialization call on startup"
---

<objective>
Establish the local-first Hono backend and initialize the Zvec vector database with persistence to the user's home directory.
</objective>

<execution_context>
@./.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-local-foundation-semantic-memory/01-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Initialize Hono Server & Environment</name>
  <files>apps/server/package.json, apps/server/src/index.ts, packages/env/src/server.ts, packages/env/package.json</files>
  <action>Setup apps/server with Hono and Bun. Configure .env validation using Zod in packages/env to include AI provider keys (Anthropic, Google, OpenAI).</action>
  <verify><automated>cd apps/server && bun run check-types || echo "Types need fixing"</automated></verify>
  <done>Server starts and environment variables validate successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Zvec Database</name>
  <files>apps/server/src/db/zvec.ts, apps/server/package.json</files>
  <action>Initialize Zvec in apps/server/src/db/zvec.ts. Set storage path to ~/.job-auto-apply/memory.db (auto-create dir). Create memories and form_templates collections.</action>
  <verify><automated>cd apps/server && bun run check-types || echo "Types need fixing"</automated></verify>
  <done>Zvec code is written and type-checks.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Health Endpoint</name>
  <files>apps/server/src/index.ts</files>
  <action>Add /health endpoint that checks Zvec connection and .env key presence.</action>
  <verify><automated>cd apps/server && bunx hono request src/index.ts /health || echo "Endpoint not ready"</automated></verify>
  <done>Health endpoint returns JSON with database connected and keys status.</done>
</task>
</tasks>

<success_criteria>
Backend foundation is solid, DB initializes correctly, and health checks pass.
</success_criteria>

---
phase: 04-enhancements-application-tracking
plan: '03'
type: execute
wave: 2
depends_on:
  - '01'
files_modified:
  - apps/server/src/lib/cover-letter.ts
  - apps/server/src/lib/pdf-generator.ts
autonomous: true
requirements:
  - COVER-01
user_setup: []
must_haves:
  truths:
    - "User can generate AI-tailored cover letters as text"
    - "User can download cover letters as PDFs"
  artifacts:
    - path: apps/server/src/lib/cover-letter.ts
      provides: AI cover letter generation
      contains: generateCoverLetter function
    - path: apps/server/src/lib/pdf-generator.ts
      provides: PDF generation from cover letter text
      contains: generateCoverLetterPDF function
  key_links:
    - from: apps/server/src/lib/pdf-generator.ts
      to: apps/server/package.json
      via: pdfkit import
---

<objective>
Create cover letter generation and PDF generation libraries.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enhancements-application-tracking/04-CONTEXT.md
@.planning/phases/04-enhancements-application-tracking/04-RESEARCH.md
@.planning/phases/04-enhancements-application-tracking/04-01-SUMMARY.md

PDFKit installed in 04-01.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cover Letter Generation Library</name>
  <files>apps/server/src/lib/cover-letter.ts</files>
  <action>
Create cover-letter.ts lib file:
1. Import: generateText from "ai", provider SDKs (anthropic, createOpenAI, google), env
2. Define ResumeData interface (from existing ResumeUpload patterns)
3. Create generateCoverLetter(provider, jobDescription, resumeData, companyName, position) function
4. Use AI prompt from RESEARCH.md - concise ~150-250 words, standard business format, summarize JD, highlight experience
5. Return generated text string

Provider handling: Use same pattern as extraction.ts for switching between anthropic/google/openai.
  </action>
  <verify>File compiles, function exported</verify>
  <done>AI cover letter generation function ready for API use</done>
</task>

<task type="auto">
  <name>Task 2: Create PDF Generator Library</name>
  <files>apps/server/src/lib/pdf-generator.ts</files>
  <action>
Create pdf-generator.ts lib file:
1. Import PDFDocument from pdfkit
2. Define CoverLetterData interface with: content (greeting, intro, body, closing, signature), candidate (name, email, phone, location), company, position
3. Create generateCoverLetterPDF(data: CoverLetterData): Promise<Buffer>
4. Generate PDF with:
   - Header: candidate name, email, phone, location
   - Date: current date in en-US long format
   - Company info: company, position
   - Content sections: greeting, intro, body, closing, signature
   - Use standard fonts (.font() calls), 12pt size, 72pt margins

Return Buffer for API streaming response.
  </action>
  <verify>File compiles, function exported</verify>
  <done>PDF generation from cover letter data functional</done>
</task>

</tasks>

<verification>
- [ ] File compiles, function exported
- [ ] PDF generation from cover letter data functional
</verification>

<success_criteria>
- User can generate cover letters using AI
- User can convert cover letter text to PDF
</success_criteria>

<output>
After completion, create `.planning/phases/04-enhancements-application-tracking/04-03-SUMMARY.md`
</output>

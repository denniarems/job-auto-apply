---
phase: 04-enhancements-application-tracking
plan: '05'
type: execute
wave: 4
depends_on:
  - '04'
files_modified:
  - apps/extension/components/ApplicationCard.tsx
  - apps/extension/hooks/useApplications.ts
  - apps/extension/entrypoints/sidepanel/Applications.tsx
  - apps/extension/entrypoints/sidepanel/App.tsx
  - apps/extension/manifest.ts
  - apps/extension/src/content-scripts/auto-capture.ts
autonomous: true
requirements:
  - TRACK-01
user_setup: []
must_haves:
  truths:
    - "User can view application history as card list with filter and search"
    - "User can update application status and delete entries"
  artifacts:
    - path: apps/extension/entrypoints/sidepanel/Applications.tsx
      provides: Application tracking list view
      contains: Card list, filter by status, text search
  key_links:
    - from: apps/extension/hooks/useApplications.ts
      to: apps/server/src/routes/applications.ts
      via: fetch calls to /api/applications
---

<objective>
Create frontend UI for application tracking.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enhancements-application-tracking/04-CONTEXT.md

Backend API: GET/POST/PATCH/DELETE /api/applications

Existing App.tsx pattern:
- Uses useState for activeTab ("main", "memories", "resumes", "settings")
- Has TabButton components in footer navigation
- Uses lucide-react icons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ApplicationCard component</name>
  <files>apps/extension/components/ApplicationCard.tsx</files>
  <action>
Create ApplicationCard component:
- Props: id, company, position, url, status, applied_date, onStatusChange, onDelete
- Display: company name (bold), position (subtitle), URL (truncated), status badge, date
- Status badge colors per requirements:
  - Applied: blue
  - Interviewing: yellow/amber
  - Offer: green
  - Rejected: red
  - Withdrawn: gray
- Actions: dropdown for status change, delete button (trash icon)
- Style: white bg, border, rounded-xl, shadow-sm, p-4

Use lucide-react for icons, tailwind for styling matching existing components.
  </action>
  <verify>Component renders without errors</verify>
  <done>ApplicationCard component ready for list use</done>
</task>

<task type="auto">
  <name>Task 2: Create useApplications hook</name>
  <files>apps/extension/hooks/useApplications.ts</files>
  <action>
Create React hook for application CRUD:
```
export function useApplications(backendUrl: string) {
  const [applications, setApplications] = useState([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const fetchAll = async () => {
    setLoading(true);
    const res = await fetch(`${backendUrl}/api/applications`);
    const data = await res.json();
    setApplications(data);
    setLoading(false);
  };

  const create = async (app: {company, position, url, status}) => {
    const res = await fetch(`${backendUrl}/api/applications`, {
      method: "POST",
      headers: {"Content-Type": "application/json"},
      body: JSON.stringify(app),
    });
    return res.json();
  };

  const updateStatus = async (id: string, status: string) => {
    const res = await fetch(`${backendUrl}/api/applications/${id}`, {
      method: "PATCH",
      headers: {"Content-Type": "application/json"},
      body: JSON.stringify({status}),
    });
    return res.json();
  };

  const remove = async (id: string) => {
    await fetch(`${backendUrl}/api/applications/${id}`, {method: "DELETE"});
  };

  return {applications, loading, error, fetchAll, create, updateStatus, remove};
}
```
  </action>
  <verify>Hook compiles without errors</verify>
  <done>useApplications hook for application CRUD operations</done>
</task>

<task type="auto">
  <name>Task 3: Create Applications list view</name>
  <files>apps/extension/entrypoints/sidepanel/Applications.tsx</files>
  <action>
Create Applications.tsx page:
- Header: "Applications" title, "Add Application" button
- Search bar: text input to filter by company/position
- Filter dropdown: All, Applied, Interviewing, Offer, Rejected, Withdrawn
- Card list: map applications to ApplicationCard components
- Empty state: "No applications tracked yet"
- Sorting: sort by applied_date descending (newest first)
- Layout: vertical list, full width cards

Integrates useApplications hook for CRUD operations.
  </action>
  <verify>Page renders without errors</verify>
  <done>Application tracking view with card list, search, and filter</done>
</task>

<task type="auto">
  <name>Task 4: Add Applications tab to App.tsx</name>
  <files>apps/extension/entrypoints/sidepanel/App.tsx</files>
  <action>
Modify App.tsx:
1. Add imports:
```
import { Applications } from "./Applications";
import { Briefcase } from "lucide-react";
```

2. Add TabButton item in footer (after "Resumes", before "Settings"):
- Applications: icon=Briefcase, label="Apps"

3. Add render condition:
```
{activeTab === "applications" && <Applications />}
```
  </action>
  <verify>Extension builds without errors</verify>
  <done>Navigation includes Applications tab</done>
</task>

<task type="auto">
  <name>Task 5: Implement auto-capture for application tracking</name>
  <files>apps/extension/manifest.ts
apps/extension/src/content-scripts/auto-capture.ts</files>
  <action>
Create content script to auto-detect job application form submissions:
1. Create apps/extension/src/content-scripts/auto-capture.ts with:
   - Detect form submission events on common job sites (LinkedIn, Indeed, Glassdoor, company career pages)
   - Extract job data from form fields or page content (company name, position, job URL)
   - Send detected application data to backend via messaging to side panel
   - Use URL patterns to identify job sites: linkedin.com/jobs, indeed.com, glassdoor.com, etc.
   
2. Update apps/extension/manifest.ts to register content script:
   - Add to content_scripts array with matches for common job sites
   - Permissions: activeTab, storage, scripting

3. Create side panel listener in Applications.tsx to receive auto-captured data:
   - Listen for messages from content script
   - Auto-populate "Add Application" form with detected data
   - Show notification "Application detected - click to save"
  </action>
  <verify>Content script compiles, manifest updated</verify>
  <done>Auto-capture detects job form submissions and prompts user to save</done>
</task>

</tasks>

<verification>
- [ ] ApplicationCard renders with proper styling and actions
- [ ] Applications page shows card list with filter/search
- [ ] App.tsx has Applications tab
</verification>

<success_criteria>
- User can view applications as card list with newest first
- User can filter by status and search by text
- User can update status and delete applications
</success_criteria>

<output>
After completion, create `.planning/phases/04-enhancements-application-tracking/04-05-SUMMARY.md`
</output>

---
phase: 04-enhancements-application-tracking
plan: '06'
type: execute
wave: 4
depends_on:
  - '04'
files_modified:
  - apps/extension/hooks/useCoverLetter.ts
  - apps/extension/components/CoverLetterPreview.tsx
  - apps/extension/components/CoverLetterEditor.tsx
  - apps/extension/entrypoints/sidepanel/CoverLetters.tsx
  - apps/extension/entrypoints/sidepanel/App.tsx
autonomous: true
requirements:
  - COVER-01
user_setup: []
must_haves:
  truths:
    - "User can generate, preview, edit, and download cover letters as PDFs"
  artifacts:
    - path: apps/extension/entrypoints/sidepanel/CoverLetters.tsx
      provides: Cover letter workflow (generate, preview, edit, download)
      contains: Generation form, preview, editor modal, download button
  key_links:
    - from: apps/extension/hooks/useCoverLetter.ts
      to: apps/server/src/routes/cover-letters.ts
      via: fetch calls to /api/cover-letters
---

<objective>
Create frontend UI for cover letter management.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enhancements-application-tracking/04-CONTEXT.md

Backend APIs:
- POST /api/cover-letters/generate - {jobDescription, resumeData, companyName, position, provider}
- POST /api/cover-letters/download - {content, company, position}
- GET /api/cover-letters/history
- DELETE /api/cover-letters/:id

Existing App.tsx pattern:
- Uses useState for activeTab ("main", "memories", "resumes", "settings")
- Has TabButton components in footer navigation
- Uses lucide-react icons
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useCoverLetter hook</name>
  <files>apps/extension/hooks/useCoverLetter.ts</files>
  <action>
Create React hook for cover letter operations:
```
export function useCoverLetter(backendUrl: string, provider: string) {
  const generate = async (jobDescription: string, resumeData: ResumeData, company: string, position: string) => {
    const res = await fetch(`${backendUrl}/api/cover-letters/generate`, {
      method: "POST",
      headers: {"Content-Type": "application/json"},
      body: JSON.stringify({jobDescription, resumeData, companyName: company, position, provider}),
    });
    if (!res.ok) throw new Error("Generation failed");
    const data = await res.json();
    return data.content;
  };

  const download = async (content: string, company: string, position: string) => {
    const res = await fetch(`${backendUrl}/api/cover-letters/download`, {
      method: "POST",
      headers: {"Content-Type": "application/json"},
      body: JSON.stringify({content, company, position}),
    });
    if (!res.ok) throw new Error("PDF generation failed");
    const blob = await res.blob();
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url;
    a.download = `CoverLetter_${company}_${position}_${new Date().toISOString().split("T")[0]}.pdf`;
    a.click();
    URL.revokeObjectURL(url);
  };

  const getHistory = async () => {
    const res = await fetch(`${backendUrl}/api/cover-letters/history`);
    return res.json();
  };

  return {generate, download, getHistory};
}
```
  </action>
  <verify>Hook compiles without errors</verify>
  <done>useCoverLetter hook for generate, download, history operations</done>
</task>

<task type="auto">
  <name>Task 2: Create CoverLetterPreview and Editor components</name>
  <files>apps/extension/components/CoverLetterPreview.tsx
apps/extension/components/CoverLetterEditor.tsx</files>
  <action>
Create CoverLetterPreview.tsx:
- Display generated cover letter text in readable format
- Show company name and position header
- Styled as document preview (white bg, padding, proper typography)

Create CoverLetterEditor.tsx:
- Modal or side panel overlay
- Textarea with cover letter content (editable)
- "Regenerate" button to call AI again
- "Download PDF" button to trigger download
- "Cancel" and "Save" buttons
- Preview toggle to see rendered version
  </action>
  <verify>Components render without errors</verify>
  <done>Cover letter preview and editing components ready</done>
</task>

<task type="auto">
  <name>Task 3: Create CoverLetters page</name>
  <files>apps/extension/entrypoints/sidepanel/CoverLetters.tsx</files>
  <action>
Create CoverLetters.tsx page with tabs or sections:
- Section 1: "Generate New" 
  - Form inputs: Job Description (textarea), Company Name, Position
  - "Generate Cover Letter" button
  - Uses useCoverLetter.generate() on submit
- Section 2: "Preview/Edit" 
  - Shows generated text in CoverLetterEditor
  - Allows editing before download
- Section 3: "History" 
  - List of previously generated cover letters
  - Each item: company, position, date, "View" and "Download" buttons

Default view: Generate New section.
  </action>
  <verify>Page renders without errors</verify>
  <done>Cover letter generation, preview, edit, and download workflow complete</done>
</task>

<task type="auto">
  <name>Task 4: Add CoverLetters tab to App.tsx</name>
  <files>apps/extension/entrypoints/sidepanel/App.tsx</files>
  <action>
Modify App.tsx:
1. Add imports:
```
import { CoverLetters } from "./CoverLetters";
import { FileSignature } from "lucide-react";
```

2. Add TabButton item in footer (after "Applications", before "Settings"):
- CoverLetters: icon=FileSignature, label="Cover"

3. Add render condition:
```
{activeTab === "coverletters" && <CoverLetters />}
```
  </action>
  <verify>Extension builds without errors</verify>
  <done>Navigation includes CoverLetters tab</done>
</task>

</tasks>

<verification>
- [ ] CoverLetterPreview shows formatted text
- [ ] CoverLetterEditor allows text editing
- [ ] CoverLetters page has generate form and history
- [ ] App.tsx has CoverLetters tab
</verification>

<success_criteria>
- User can generate AI cover letter from job description
- User can edit cover letter before downloading
- User can download cover letter as PDF with proper filename
- User can view cover letter history
</success_criteria>

<output>
After completion, create `.planning/phases/04-enhancements-application-tracking/04-06-SUMMARY.md`
</output>

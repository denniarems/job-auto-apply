---
phase: 04-enhancements-application-tracking
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/src/db/lancedb.ts
  - apps/server/src/routes/applications.ts
  - apps/server/src/routes/cover-letters.ts
  - apps/server/src/lib/cover-letter.ts
  - apps/server/src/lib/pdf-generator.ts
  - apps/server/src/index.ts
  - apps/server/package.json
autonomous: true
requirements:
  - COVER-01
  - TRACK-01
user_setup: []
must_haves:
  truths:
    - "User can generate AI-tailored cover letters as downloadable PDFs"
    - "User can view and track job applications they've applied to"
    - "Cover letters are saved to local history and linked to applications"
  artifacts:
    - path: apps/server/src/db/lancedb.ts
      provides: applications and cover_letters tables
      contains: applicationsTable, coverLettersTable exports
    - path: apps/server/src/routes/applications.ts
      provides: CRUD API for application tracking
      exports: GET /api/applications, POST, PATCH, DELETE
    - path: apps/server/src/routes/cover-letters.ts
      provides: Cover letter generation and management
      exports: POST generate, POST download, GET history
    - path: apps/server/src/lib/pdf-generator.ts
      provides: PDF generation from cover letter text
      contains: generateCoverLetterPDF function
  key_links:
    - from: apps/server/src/routes/applications.ts
      to: apps/server/src/db/lancedb.ts
      via: applicationsTable import
    - from: apps/server/src/routes/cover-letters.ts
      to: apps/server/src/lib/cover-letter.ts
      via: generateCoverLetter import
    - from: apps/server/src/routes/cover-letters.ts
      to: apps/server/src/lib/pdf-generator.ts
      via: generateCoverLetterPDF import
---

<objective>
Add database tables and backend APIs for cover letter generation and application tracking.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enhancements-application-tracking/04-CONTEXT.md
@.planning/phases/04-enhancements-application-tracking/04-RESEARCH.md

Existing LanceDB pattern from apps/server/src/db/lancedb.ts:
- Uses tables for memories and form_templates
- Creates tables with __init__ placeholder rows
- Exports table instances for route use

Existing route pattern from apps/server/src/routes/memories.ts:
- CRUD operations with Hono router
- Uses uuid for ID generation
- BigInt handling for timestamps
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install PDFKit dependency</name>
  <files>apps/server/package.json</files>
  <action>
Install pdfkit and @types/pdfkit:
```
cd apps/server && npm install pdfkit @types/pdfkit
```
  </action>
  <verify>npm list pdfkit shows installed</verify>
  <done>pdfkit dependency added to package.json</done>
</task>

<task type="auto">
  <name>Task 2: Extend LanceDB with applications and cover_letters tables</name>
  <files>apps/server/src/db/lancedb.ts</files>
  <action>
Add two new table exports and initialization:
1. Export: `applicationsTable: lancedb.Table` and `coverLettersTable: lancedb.Table`
2. In initDb(): create applications table with schema:
   - id, company, position, url, status, applied_date, created_at
3. Create cover_letters table with schema:
   - id, application_id, company, position, content, generated_at

Use the existing pattern from memoriesTable creation (try/catch with "already exists" handling).

Reference existing code:
```typescript
// Add exports at top
export let applicationsTable: lancedb.Table;
export let coverLettersTable: lancedb.Table;

// In initDb(), add after templatesTable creation:
const appData = [{
  id: "__init__",
  company: "__init__",
  position: "__init__",
  url: "__init__",
  status: "__init__",
  applied_date: 0n,
  created_at: 0n,
}];
```
  </action>
  <verify>File compiles, tables exported</verify>
  <done>LanceDB has applications and cover_letters tables with CRUD-ready exports</done>
</task>

<task type="auto">
  <name>Task 3: Create Application CRUD API</name>
  <files>apps/server/src/routes/applications.ts</files>
  <action>
Create new route file with Hono router:
- POST / - Create application (company, position, url, status, applied_date)
- GET / - List all applications (sorted by date desc)
- GET /:id - Get single application
- PATCH /:id - Update status
- DELETE /:id - Delete application

Schema:
- id: uuid v4
- company: string
- position: string  
- url: string
- status: "Applied" | "Interviewing" | "Offer" | "Rejected" | "Withdrawn"
- applied_date: BigInt (timestamp)
- created_at: BigInt (timestamp)

Use existing memories.ts patterns: uuid import, BigInt handling, table operations.
  </action>
  <verify>Routes registered, compiles without error</verify>
  <done>Full CRUD API for application tracking operational</done>
</task>

<task type="auto">
  <name>Task 4: Create Cover Letter Generation API</name>
  <files>apps/server/src/lib/cover-letter.ts</files>
  <action>
Create cover-letter.ts lib file:
1. Import: generateText from "ai", provider SDKs (anthropic, createOpenAI, google), env
2. Define ResumeData interface (from existing ResumeUpload patterns)
3. Create generateCoverLetter(provider, jobDescription, resumeData, companyName, position) function
4. Use AI prompt from RESEARCH.md - concise ~150-250 words, standard business format, summarize JD, highlight experience
5. Return generated text string

Provider handling: Use same pattern as extraction.ts for switching between anthropic/google/openai.
  </action>
  <verify>File compiles, function exported</verify>
  <done>AI cover letter generation function ready for API use</done>
</task>

<task type="auto">
  <name>Task 5: Create PDF Generator</name>
  <files>apps/server/src/lib/pdf-generator.ts</files>
  <action>
Create pdf-generator.ts lib file:
1. Import PDFDocument from pdfkit
2. Define CoverLetterData interface with: content (greeting, intro, body, closing, signature), candidate (name, email, phone, location), company, position
3. Create generateCoverLetterPDF(data: CoverLetterData): Promise<Buffer>
4. Generate PDF with:
   - Header: candidate name, email, phone, location
   - Date: current date in en-US long format
   - Company info: company, position
   - Content sections: greeting, intro, body, closing, signature
   - Use standard fonts (.font() calls), 12pt size, 72pt margins

Return Buffer for API streaming response.
  </action>
  <verify>File compiles, function exported</verify>
  <done>PDF generation from cover letter data functional</done>
</task>

<task type="auto">
  <name>Task 6: Create Cover Letter API Routes</name>
  <files>apps/server/src/routes/cover-letters.ts</files>
  <action>
Create new route file:
- POST /generate - Generate cover letter text using AI (jobDescription, resumeData, companyName, position, provider)
- POST /download - Generate PDF and stream to client (content, company, position) - returns file stream with Content-Disposition attachment
- GET /history - List saved cover letters
- DELETE /:id - Delete cover letter

For /download: 
- Parse content into sections (greeting, intro, body, closing, signature) - can split by double newline
- Get candidate info from resume memories or use placeholder
- Generate PDF Buffer
- Return with headers: Content-Type: application/pdf, Content-Disposition: attachment; filename="CoverLetter_{company}_{position}_{date}.pdf"

Link to applications via application_id in cover_letters table.
  </action>
  <verify>Routes registered, compiles without error</verify>
  <done>Cover letter generation, PDF download, and history CRUD operational</done>
</task>

<task type="auto">
  <name>Task 7: Register new routes in index.ts</name>
  <files>apps/server/src/index.ts</files>
  <action>
Add to imports:
```
import applications from "./routes/applications";
import coverLetters from "./routes/cover-letters";
```

Add route registration:
```
app.route("/api/applications", applications);
app.route("/api/cover-letters", coverLetters);
```
  </action>
  <verify>Server starts without errors</verify>
  <done>New API routes accessible at /api/applications and /api/cover-letters</done>
</task>

</tasks>

<verification>
- [ ] pdfkit installed
- [ ] LanceDB has applicationsTable and coverLettersTable exports
- [ ] /api/applications CRUD endpoints working
- [ ] /api/cover-letters/generate returns AI-generated text
- [ ] /api/cover-letters/download returns PDF file
- [ ] Server starts with new routes
</verification>

<success_criteria>
- User can create application entries via API
- User can generate cover letters using AI
- User can download cover letters as PDFs with proper filename format
- All data persists in local LanceDB
</success_criteria>

<output>
After completion, create `.planning/phases/04-enhancements-application-tracking/04-01-SUMMARY.md`
</output>
